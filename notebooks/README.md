# Notebooks

Exploration and storytelling only. **Do not put core backtest logic here.**

Suggested naming:

- `01_data_exploration.ipynb`
- `02_baseline_checks.ipynb`

Core logic belongs in `src/`; run full pipeline via `python scripts/run_all_stages.py`.
