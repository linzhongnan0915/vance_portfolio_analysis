# Dashboard screenshots

Store README-ready dashboard captures here.

| File | Purpose |
|------|---------|
| `dashboard_overview.png` | Main preview linked from root `README.md` |

This folder is tracked in git; generated pipeline outputs under `output/` are not.
